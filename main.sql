    -- Create the table
    CREATE TABLE ExampleTable (
        ID int,
        Name nvarchar(255)
    );

    -- Insert values into the table
    INSERT INTO ExampleTable (ID, Name) VALUES (1, '123');
    INSERT INTO ExampleTable (ID, Name) VALUES (2, '456');
    INSERT INTO ExampleTable (ID, Name) VALUES (3, '789');

-- Select and view all data from the tabl